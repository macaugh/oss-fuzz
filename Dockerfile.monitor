FROM python:3.11-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir requests

# Copy monitor script
COPY monitor/monitor.py /app/

# Create state directory
RUN mkdir -p /app

CMD ["python", "-u", "/app/monitor.py"]
